// Primitive data types are `String`, `Number`, `Boolean`, `undefined` , `null`
let name1 = 'Brian'; //String literal
let age = 30; //Number literal
let isApproved = true; //Boolean literal
let firstName1; //Undefined will be declared
let selectedColor = null;// clear the value of the variable

console.log(name1);
console.log(age);
console.log(isApproved);
console.log(firstName1);
console.log(selectedColor);
// In the console of browser we can check the `typeof <variable-name>` for testing the value
// type of the variables will be determined during the runtime based on the assigned value